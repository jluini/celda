[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://tools/grog/grog_script.gd" type="Script" id=1]

[resource]
resource_name = "main"
script = ExtResource( 1 )
code_or_filename = "init:
	.set stage=1

	.set take_desc_attempts=0
	.set number=0
	.set door_seen=false
	.set poster_was_taken=false
	
	celda/exit.disable
	
	exit/claw1.disable
	exit/claw2.disable
	exit/claw3.disable
	
	exit/mask2.play red
	exit/mask3.play green
	
	.set current_mask=\"\"
	
	.set bust_turn = false

start:
	.load_room celda
	.set music=sufre
	
	you.teleport to=pos3
	.curtain_up
	.enable_input


fallback/walk_to: # goes but nothing more

fallback/talk_to: TK # does nothing

fallback/use: TK # does nothing

fallback/look_at:
	you.say CELDA_ESCAPE_FALLBACK_LOOK

fallback/open: TK
	you.say CELDA_ESCAPE_FALLBACK_CANT_OPEN
fallback/close: TK
	you.say CELDA_ESCAPE_FALLBACK_CANT_CLOSE

fallback/pick_up: TK
	you.say CELDA_ESCAPE_FALLBACK_PICK_UP

fallback/use_with(*): TK
	you.say \"Eso no parece funcionar.\"
	"
